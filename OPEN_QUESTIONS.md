# Open Questions

Priorities: `Critical` changes the architecture; `High` affects multiple interfaces; `Medium` affects one workflow; `Low` is deferrable implementation detail.

| ID | Priority | Question | Needed from | Blocks | Status |
|---|---|---|---|---|---|
| Q-001 | Critical | Is the supplied technical specification an approved greenfield design, a draft, or documentation of objects in another system? | Project manager/architect | All implementation | **CLOSED 2026-08-04 (D-019)** — no `ZCNF*` objects exist in QS4. Greenfield, verified by system observation |
| Q-002 | Critical | What is the exact DEV/QAS/PRD landscape and is Gateway embedded or hub? | Basis | Development setup | **Largely closed 2026-08-04 (D-018)** — S/4HANA 2022 on-premise; three systems **DS4 / QS4 / PS4**. The vendor spec's "two-system DEV→PRD" was wrong. Gateway topology still to confirm — **new evidence 2026-08-05**: six services registered alias `LOCAL` (embedded), but SEGW Service Maintenance lists **`E8H_000`** in all six projects, suggesting a hub system E8H may also exist. Ask Basis what E8H is |
| Q-003 | Critical | What does “DI” represent in S/4: outbound delivery, custom object, or instruction preceding delivery? | SD/business | Delivery APIs | **Resolved and clarified 2026-08-11 — see D-007:** Delivery Instruction / DI and SAP outbound delivery are interchangeable project terms for the same document; DI No. = `LIKP-VBELN` |
| Q-004 | Critical | What exact reference and response identifier does Submit MIGO use, and how are partial receipts allocated? | MM/SD | MIGO design | **Largely answered 2026-08-04/11 (D-021/D-052/D-054)** — plant→depot movement: STO (`EBELN`) → outbound delivery → GI → in transit → GR **mvt 101 against the delivery** (`MATDOC-VBELN_IM`). The modal separates prior history from the new classification delta. Accepted classifications may increase stock; rejected `DMG`/`STG` must not. Residual: entry reference, material-document-versus-“MRN” returned label, exact accepted-bucket posting rules, rejection persistence, concurrency/version guard, reversals and C-14 terminology. Pending-ledger destination is split to Q-066 |
| Q-005 | Critical | Which product reads come from S/4 and which come directly from Datasphere? | Architect/DSP/CPI | Source design | Open — **partially answered 2026-08-03**: Total Pending Orders is served by Commerce/Hybris T1, not S/4 (`SRC-MTG-20260803-01` @26:52–27:29), independently corroborating the architecture diagram. A field-level source mapping is being built by S2 — obtain it (A-1). See Q-036 |
| Q-006 | High | What is the first formally assigned interface and its signed-off acceptance criteria? | Manager | Start of work | Open — **working sequence only, 2026-08-10:** focus on MIGO and trade/non-trade order fulfilment; defer STO (`SRC-MTG-20260810-02 @24:19–24:57`). This is anonymous-speaker sequencing, not formal assignment or acceptance. Obtain the controlled BRD/Figma revision and owner sign-off |
| Q-007 | High | What is the system of record for depot/warehouse assignment, material alias, vehicle, transporter, driver, and geography? | Business/Commerce/functional | Field mapping | Open — 10 Aug UI intent has transporter ID/name prefilled from a transporter master and multi-warehouse assignment driving the user's view (`SRC-MTG-20260810-01 @08:40–09:05, @26:15–26:33`); authority and synchronization remain unknown |
| Q-008 | High | Is invoice creation a synchronous atomic command or a staged/asynchronous process? | SD/architect/CPI | Invoice architecture | Open — Create DI and Submit MIGO are intended to return immediate SAP identifiers (`SRC-MTG-20260810-02 @25:41–27:43`), but the invoice Document Flow explicitly models completed/error/processing and background work (`SRC-MTG-20260810-01 @01:40–02:09`). Separate command acknowledgement from the multi-stage invoice orchestration; Q-032 still requires measurement |
| Q-009 | High | Does CPI own GSP/E-Way Bill external calls, or is ABAP expected to call directly? | Architect/CPI/Basis | E-doc design | Open |
| Q-010 | **Critical** | What prevents duplicate MIGO, delivery, PGI, and invoice processing? | Architect/CPI/ABAP | Write APIs | Open — **evidenced and escalated 2026-08-05 (D-031)**: *nothing does today.* Neither existing write API carries a request ID, duplicate check or replay guard. `ZCRM_SO_REJECT` is safe only by accident (setting a rejection reason is naturally idempotent); `BAPI_PO_CREATE1` in `ZMM_SCRUM_SER_PO` will create two POs from the same payload. Create-DI, MIGO and invoice are all non-idempotent — the existing pattern does not transfer |
| Q-011 | High | What common API error schema and HTTP mapping is approved? | Integration architect | All APIs | Open |
| Q-012 | High | What logging/correlation standard is required across Commerce, CPI, SAP, and Datasphere? | Architect/CPI | Supportability | Open |
| Q-013 | High | Which standard SAP APIs are preferred/allowed in this S/4 release? | ABAP lead/architect | Runtime provisioning and exception policy | **Design-time answered 2026-08-15 (D-057):** primary released candidates are `API_MATERIAL_DOCUMENT_SRV`, `API_OUTBOUND_DELIVERY_SRV;v=2`, `API_BILLING_DOCUMENT_SRV`, `API_PURCHASEORDER_PROCESS_SRV` and `API_MATERIAL_STOCK_SRV`. This is not a claim that they are active or callable. Q-045/Q-067 own activation and runtime proof; Q-068 owns whether internal Fiori services may be integration endpoints |
| Q-014 | Medium | What does “available stock” mean: unrestricted physical, ATP, batch stock, or modeled availability? | MM/business | Stock API | **API-05 largely answered 2026-08-15 (D-060):** it is the physical/book-stock population at material × plant × storage-location grain, led by `API_MATERIAL_STOCK`. MM must still name included `InventoryStockType` values. The separate outbound allocation/ATP question remains Q-038 and must not be collapsed into API-05 |
| Q-015 | Medium | Is FIFO batch selection decided in S/4 or proposed by the portal? | MM/SD | Invoice flow | Open |
| Q-016 | Medium | Where are invoice, E-Way Bill, and e-Invoice files stored and retrieved? | SD/e-doc/CPI | Downloads | Open |
| Q-017 | Medium | Does inventory reconciliation post SAP physical inventory differences or only store/submit a portal reconciliation, and which system owns its audit? | MM/business | Reconciliation | **Read side resolved by D-051:** current v1.7 API-05 returns live system stock at product × storage-location grain for the selected plant; no DI context and no mandatory material filter. **Command side remains open:** “Post” effect, approval, reversal and authoritative audit store are unknown. Validate material UoM conversion; do not hardcode `1 bag = 0.05 MT` globally |
| Q-018 | Medium | What are Datasphere object names, grains, lineage, and refresh SLAs? | Datasphere team | Analytical interfaces | Open |
| Q-019 | Medium | What package, namespace, message class, exception, and transport standards should both ABAPers share? | ABAP lead/Basis | Implementation consistency | Open |
| Q-020 | Low | Is approved read-only SAP GUI scripting available per user in DEV? | Basis/security | Optional automation | Open |
| Q-021 | Critical | What does KDS mean, and what are the approved code dictionaries for business segment, product families, material groups, sales organisations, and code `1300`? | SD/business master-data owners | Field mapping | **Substantially resolved 2026-08-03** — KDS = Key Data Structure; the catalogue arrived as `SRC-DOC-20260803-02` (11 sheets). MVGR1–5, KDGRP, MFRGR, KONDM, Nielsen, SPI, T001L, TVKWZ, TVKBZ all now documented in `DOMAIN_GLOSSARY.md`. Residual: `1300` conflict (Q-040) |
| Q-022 | High | What exact SAP fields and rules implement FTP versus **FTB** versus EX-works, including pricing/freight and pickup code? | SD/FI | Invoice contract | Open — there are three shipping types (Q-034) and pickup-code behavior conflicts (Q-033/C-8). Current UI intent inherits the term from the dealer order, displays it prefilled, and does not allow portal editing even though SAP can change it (`SRC-MTG-20260810-01 @00:25–00:50`); field/pricing rules still need SD validation |
| Q-023 | High | Is storage location selected and persisted at DI creation, and does it become immutable? | SD/business/ABAP | DI and invoice | Open |
| Q-024 | High | Should stock and route/freight availability block DI creation, and in which system is the rule enforced? | Business/SD/architect | DI create | Open |
| Q-025 | High | What is SPI, which SAP object/field owns it, and how does it affect freight and storage? | SD/LE | Invoice/shipment | **Resolved 2026-08-03** — SPI = Special Procurement Indicator, mapped storage-location↔SPI in custom table `ZLETSPIMAP`; governs SLoc eligibility for MIGO/Stock/Invoice. Not shipping point, not SCPI |
| Q-026 | High | Which SAP documents/APIs correspond to PGI, shipment, shipment cost, billing, e-Invoice, and E-Way Bill stages, in what order, and with which completion model? | SD/LE/tax/ABAP/CPI | Invoice orchestration | Open — the UI presents one confirmation after LR/GR/vehicle/driver inputs, but also exposes background Processing and failure states (`SRC-MTG-20260810-01 @00:51–02:09`). This is user-journey evidence, not proof of atomicity, document order or API choice |
| Q-027 | High | Where are the historical trade, non-trade, and STO recordings, and who will provide controlled SD/MM KT? | Manager/Harish | Semantic model | Open — Sujal remains the first named KT candidate (`SRC-MTG-20260803-01 @23:21`) and is repeatedly named as the BRD/Figma access/walkthrough contact on 10 Aug (`SRC-MTG-20260810-02 @08:23–08:50, @25:05–26:31`). Obtain the actual artifacts and an owner-approved KT record |
| Q-028 | Medium | Is batch ranking FIFO, largest available quantity, or configurable by process? | MM/SD | Batch proposal | Open |
| Q-029 | Medium | Which invoice identifier is needed in each UI/API: billing document, ODN, accounting document, or IRN? | SD/FI/tax | Status/download | Open |
| Q-030 | Medium | Does the Commerce layer retain 60 days, and what should come from Datasphere for older history? | Commerce/Datasphere | Reporting source | Open |
| Q-031 | Critical | Now that DI = outbound delivery (D-007), which predecessor creates it — sales order (`BAPI_OUTB_DELIVERY_CREATE_SLS`) or STO (`BAPI_OUTB_DELIVERY_CREATE_STO`)? Does trade vs non-trade vs STO use different predecessors? | SD/business | IF-003/004/014/015/018/019; resolves C-7 | Open — 10 Aug says T1 contains trade/non-trade orders but not STO, and walks the STO branch as PO/STO → DI → normal fulfilment while deferring it from the immediate build (`SRC-MTG-20260810-01 @14:38–16:43`; `SRC-MTG-20260810-02 @15:22–15:42, @24:19–24:57`). Exact SAP document types/BAPIs still require SD confirmation |
| Q-032 | Critical | What is SAP's actual measured latency for the invoice-generation chain (PGI → shipment → shipment cost → billing → e-Invoice → E-Way Bill)? | Tech team/Basis/SD | Q-008; V-04; API-06 orchestration shape; whether the portal needs polling | Open — nobody had measured it on 3 Aug. The 10 Aug UI still models Processing/failure, so immediate MIGO/DI numbers do not answer invoice-chain latency (`SRC-MTG-20260810-01 @01:40–02:09`; `SRC-MTG-20260810-02 @25:41–27:43`) |
| Q-033 | High | Pickup code and Incoterm: is the field **disabled in FTP** or **present only in FTP**? Both were stated in the same session (C-8), and both conflict with the 28 July record | SD/functional | Q-022; Incoterm-conditional field logic; shipment payload | Open — conflict C-8 |
| Q-034 | High | What is FTB ("Freight to Bill"), how does it differ from FTP, and which SAP Incoterm/pricing/freight configuration implements each? Three shipping types exist (FTP/FTB/EX), not two | SD/FI | Q-022; DOMAIN_GLOSSARY; shipment and billing contracts | Open — raised 2026-08-03 |
| Q-035 | High | Does SAP need to expose a **queryable invoice-generation status** (number / error / processing), and where does a *failed* invoice get retried? D-010 removes the DI from In-Progress on failure too, leaving no retry surface specified | Architect/CPI/SD | API-06; Q-010 idempotency; Document Flow; Invoice Management | Open — 10 Aug confirms Document Flow has number/success, cross/failure and Processing, while Generated Invoices contains only zero-error records (`SRC-MTG-20260810-01 @01:40–02:09, @03:36–04:05`). Status need is stronger; failed-item ownership/retry remains absent |
| Q-036 | Medium | For each dashboard tile, which system serves the figure and at what grain/freshness? | S2/architect | Q-005; source-of-record matrix | Open — Option C maps orders/deliveries/invoices to T1 and MRN/STO/ageing to DSP→T2. The 10 Aug walkthrough defines STO-in-transit, credit-free pending, In Process, Billed Quantity, ePOD and ageing, and settles ageing to D-1 (`SRC-MTG-20260810-01 @22:02–24:48`), but not every object/query/SLA |
| **Q-037** | **Critical** | **Which exact interfaces move SAP-created deliveries/invoices into T1, and how do the full-document projection and stage-gate feed relate?** | Architect / CPI / Commerce / CRM | Whole read model under Option C; outbound SAP scope; read-after-write UX | Open — system evidence proves pull-shaped `ZCRM_STAGEGATE_SRV` progression. The 10 Aug KT separately claims an existing trigger-shaped SAP→CPI→T1 projection for order/DI/invoice changes and says the synchronous SAP response supplies the immediate DI number before T1 catches up (`SRC-MTG-20260810-02 @20:45–21:50, @25:41–26:06`). Identify the interface/output/event, owner, payload coverage, correlation, retry and measured latency; do not assume the two paths are one |
| **Q-038** | **Critical** | **What live S/4 read/validation supplies stock and batch eligibility for outbound fulfilment?** Stock Ageing/dashboard data is display-only; it must not authorize allocation | Architect / MM / SD | D-002/D-003 batch rules; outbound fulfilment | Open — **do not collapse this into current v1.7 API-05.** D-051 establishes API-05 as the physical-inventory product × storage-location book-stock listing. The validated outbound screen still needs batch eligibility/FIFO validation; `API_PRODUCT_AVAILY_INFO_BASIC` is only an unproven plant-level ATP candidate for that separate need |
| **Q-039** | **High** | **How does Datasphere obtain Pending MRN, STO List and Stock Ageing — raw table replication (SLT/CDC) or a CDS/semantic view?** "Pending" is derived logic, not a stored field, and `ZLE526` already implements it in SAP. Independent reimplementation in DSP will drift from SAP's definition | Datasphere team / Architect | Q-005, Q-018; API-01 scope; report parity | **Mechanism answered 2026-08-05 (D-038)** — extraction-enabled ABAP CDS views → ODP delta queue → HANA SDI → Datasphere tenant `DWCTZ8YIB`. **But the three feeds in question have no queue at all.** Only billing header and customer master are subscribed. The drift risk stands, and the prior question becomes Q-053: who builds the missing views |
| **Q-040** | **Medium** | Are `1000` and `1300` sales organisations, company codes, or both? Relayed as sales orgs; the KDS document lists them as company codes (`T001`) and shows only sales org `1000` (`TVKO`). Affects API scoping and authorization | SD / FI | Org scoping on every API; authorization | Open — conflict **C-10**, raised 2026-08-03 |
| **Q-041** | **Medium** | Confirm ODN = Official Document Number (India GST statutory numbering), which config object generates it, whether sales orgs/GST registrations have separate series, and whether the portal should display ODN rather than the SAP billing document number | SD / FI / Tax | Q-029; API-06 response; API-07 input | Open — raised 2026-08-03 |
| **Q-042** | **High** | With DigiGST confirmed (D-017), what is the ABAP integration surface and approved correction/cancel/regenerate boundary for current v1.7 API-06 E-Way Extension and API-07 Invoice Correction? | Tax / Basis / Architect | API-06/API-07 implementation route and effort | **Infrastructure answered (D-035):** existing `EY_*` destinations cover generate/cancel/extend/Part-B. The validated correction screen narrows editable data to Part A transporter name/ID/distance and Part B mode/vehicle type/vehicle number. Still identify the invoking FM/BAdI/service and confirm whether Update Details cancels/regenerates either document; tracked operationally under Q-071 |
| **Q-043** | **Medium** | Several needs already have client custom reports. Should the APIs wrap or reuse these rather than rebuild composites? | ABAP lead / SD / MM | API-01 classification and effort | **Answered 2026-08-04 — mixed, and better than feared.** The T-codes are report programs (not callable), **but underneath them:** MRN pending reads an existing CDS view **`zsd_mrn_pending_cds_opt`** (plus `zle_di_inv_details`, `zle_mrn_goods_reciet_cds`), and pending orders calls an **RFC-enabled FM `ZSD_PENDING_ORDER_FM`** returning `zsd_st_pending_order_out`. Both directly consumable. Still to check: `ZLE_MRN_REPORT`, `ZSD_SALES_REGISTER_N`, `ZMM_INVENTORY_AGING_REPORT` |
| **Q-044** | **High** | A storage location permits **multiple** SPIs (`ZLETSPIMAP`; GDF permits five). What determines which SPI applies to a delivery item — material, process, order type, or user selection? Nothing in the design specifies it | SD / LE | API-03 payload; `LIPS-SDABW` derivation; the DI creation screen | Open — raised 2026-08-04 from D-020 |
| **Q-045** | **Critical** | Can Basis register/activate the five leading released services in **DEV**, with correct version, alias, ICF and roles: `API_MATERIAL_DOCUMENT_SRV`, `API_OUTBOUND_DELIVERY_SRV;v=2`, `API_BILLING_DOCUMENT_SRV`, `API_PURCHASEORDER_PROCESS_SRV`, `API_MATERIAL_STOCK_SRV`? | Basis / Security | All standard-first runtime proof | **CLOSED 2026-08-25 (D-062)** — all five were transported, active and callable in QS4/700. Service activation no longer proves business equivalence; remaining runtime and enhancement questions stay under Q-067 and Q-073/Q-078 |
| **Q-046** | **High** | For each genuine gap, should the callable surface be an approved existing V2/V4 service, a narrow RAP/SEGW endpoint, or a BAPI-backed class invoked through the established integration pattern? | ABAP lead / Architect / CPI | Shipment calculation, statutory commands and any disallowed internal-service use | Open — no longer a repository-wide “SEGW or RAP” debate. Standard services are used as delivered. The protocol choice applies only to proven gaps and must match CPI/client standards |
| **Q-047** | **Medium** | `ZLE526_EXCLUDE_DI` in `TVARVC` excludes specific deliveries from the pending-MRN result. What is this workaround for, is it still needed, and must the API honour it? | MM / SD | API-01 correctness | Open — raised 2026-08-04 |
| **Q-048** | **Medium** | Which quantities and cardinalities does the portal order contract use? Is “pending” `BAL_QTY`, can one order have multiple material lines, and can it create multiple DIs? | SD / T1 | D-001/D-026; pending-order and DI contracts | Open — source code proves a richer quantity chain. The 10 Aug KT confirms one order→many DIs but asserts exactly one product/line across trade/non-trade/STO (`SRC-MTG-20260810-02 @19:31–20:18`); validate that rule against BRD/T1/SAP before implementation |
| **Q-049** | Low | **What is `E8H_000`?** Appears under Service Maintenance in all six SEGW projects | Basis / Architect | — | **CLOSED 2026-08-05** — Siddharth: routinely present in SEGW Service Maintenance across prior workflows, carries no significance (`SRC-SID-20260805-01`). The "hub Gateway" reading was an unconfirmed inference and is withdrawn. **Gateway embedded-vs-hub remains open under Q-002** — it never depended on this observation |
| **Q-050** | **Critical** | **What is the compensating action when a multi-stage orchestration fails partway?** `ZMM_SCRUM_SER_PO` (D-030) commits the PO, then the service sheet, then the invoice — with no rollback across stages. A failure at stage two leaves an orphaned PO. API-06 has the same shape across four SAP units of work and two external calls | Architect / SD / ABAP | API-06 design; Q-035 retry surface; NFR-01 | Open — raised 2026-08-05 |
| **Q-051** | **Medium** | What do parameters `chk`, `chk1`, `chk2` do? They are passed straight from `ZSD_PENDING_ORDER_REP_PP` into `ZSD_PENDING_ORDER_FM` and are behaviour switches on the pending-order logic. Text elements not captured | ABAP lead / SD | Pending-order API request contract | Open — raised 2026-08-04 |
| **Q-052** | **Medium** | **Is the QS4 code identical to PS4?** All findings D-021..D-037 are read from QS4. Objects arrived by transport from DS4, but transport-to-production status is unconfirmed. Verify via `SE24` → Utilities → Versions, or `SE03` + `STMS` import status | ABAP lead / Basis | Confidence level of every source-derived decision | Open — raised 2026-08-05. Does not affect *structural* findings (patterns, absent cross-cutting concerns), only line-level detail |
| **Q-053** | **High** | **Who builds the SAP-side CDS extraction views for the remaining Datasphere feeds?** | Architect / ABAP lead / DSP team | Option C viability; unscoped SAP-side effort | Open — no delta queues exist for Pending MRN, STO List or Stock Ageing (D-038). The 10 Aug MIGO flow explicitly depends on DSP/T2 display → SAP submit → SAP data re-synced through DSP/T2 (`SRC-MTG-20260810-02 @13:30–14:32`), increasing the implementation gap without naming an owner |
| **Q-054** | **High** | **Who owns the ILMS tables (`ZLETILMS*`), and is stage-gate visibility in CNF scope?** Stage-gate data comes from ILMS, not SAP document flow (D-033), and `ZLETILMSTRANS` carries a `stageid`. The 4 Aug meeting contradicted itself on whether CNF needs stage gates at all (**C-13**) | LE / Business / Architect | Stage-gate scope; whether any CNF API touches ILMS | Open — raised 2026-08-05 |
| **Q-055** | **Medium** | Are the SAP-side calls to the `EY_*` statutory destinations (D-035) invoked from ABAP directly, and via which function modules/BAdIs? Which objects implement the validated Part A/Part B correction set, extension and cancel/regenerate? | Tax / ABAP lead | Current v1.7 API-06/API-07 build shape; refines Q-042/Q-071 | Open — the validated production screen confirms the functional correction fields (`SRC-FIG-20260811-07`, D-053), but no SAP object is named and the success toast does not prove statutory regeneration |
| **Q-056** | **Medium** | **Is ePOD in CNF scope, for which movement/order types, and which system issues, validates and persists its code and completion status?** It is described as Electronic Proof of Delivery, code-based, desired but inactive at many warehouses, with validation not yet discussed | Business / LE / Architect / Commerce | ePOD dashboard tile; delivery completion semantics | Open — raised 2026-08-10 from `SRC-MTG-20260810-01 @22:40–23:34` |
| **Q-057** | **Medium** | **Who sends a corrected invoice/e-document to the driver by SMS, what event triggers it, and where are delivery/retry/audit status and consent/PII controls held?** | Product / CPI / Tax / Security | Invoice/E-Way correction; notification contract | Open — Continue and Update are both described as the trigger (`SRC-MTG-20260810-01 @10:01–11:10`) |
| **Q-058** | **High** | **An unlisted SAP interface exists: return the valid storage locations for a plant.** *"When I did initiate me go, I got all the storage locations against that plant **which you are returning**"* (`SRC-MTG-20260810-02 @14:00`). Source is presumably `T001L`, possibly SPI-filtered via `ZLETSPIMAP`. It has no API ID, no effort estimate and is not in API-01..API-10 | Architect / ABAP lead | API-01 prerequisite; the MIGO screen cannot render without it | Open — raised 2026-08-10 |
| **Q-059** | **High** | **Does movement mode (rail vs road) change which fields are populated or mandatory?** `RSD` denotes a rail-siding depot; `LR/GR` accommodates Lorry Receipt and the rail equivalent. `ZLE_MRN_PENDING_REPORT` filters on `VTTK-VSART` (`s_shpmod`) and `ZSD_PENDING_ORDER_REP_PP` carries `VSBED`/`VSBED_DES` ("Mode Of Dispatch") — both were omitted from the drafted contracts. On a rail receipt, is `Vehicle` a wagon/rake number, or empty? | LE / SD | API-01/02/03 mandatory-field rules; vehicle format validation | Open — raised 2026-08-10 |
| **Q-060** | **Medium** | **What is the authoritative bags↔MT conversion, and where does it come from?** Inventory reconciliation accepts bags and displays MT (`4000 → 200 MT`, implying 50 kg). This must derive from material UoM conversion (`MARM`), not a constant — pack size is a material attribute (`MVGR4`) and varies | MM | Q-017; inventory reconciliation payload; any quantity shown in bags | Open — raised 2026-08-10 |
| **Q-061** | **High** | **What is `Order ID` on the portal?** Displayed as `4700987` — seven digits. Sales orders in QS4 are ten digits beginning `23`. This is a contract, a CRM-side reference, or another object. It determines what `PredecessorDocument` actually receives on current v1.7 API-02 | SD / Sujal | API-02 request; Q-031 | Open — raised 2026-08-10 from `SRC-FIG-20260810-01` |
| **Q-062** | **High** | **Portal login is mobile number + OTP** (`SRC-MTG-20260810-02 @10:42`), and the UI shows a user with a separate "SAP Code". How does a portal identity resolve to a SAP principal? If calls arrive as one technical user, `ZLE_PLNT` (D-024) cannot filter per person and depot scoping moves entirely into Commerce | Architect / Basis / Security | NFR-05; D-036; every read API's authorization scope | Open — raised 2026-08-10 |
| **Q-063** | **Critical** | **Is v1.7 approved as the numbered solution baseline, and how do its API IDs map to the seven formal T2→S/4 commands?** Current v1.7 maps API-01..API-07 to the seven commands, adds API-08..API-10 as S/4→T2 STO feeds, and retains API-11/API-12 as candidates | Manager / Architect / CPI / ABAP lead | C-16; formal assignment and acceptance criteria | Open — the numbering is internally stable and source-hashed, but approval/scope ownership for the three STO feeds and two candidates still requires the named owners |
| **Q-064** | **High** | **What is the authoritative Pending Order direction and OCC ownership?** Architecture says T1→T2; interface-summary row 11 says T2→T1 | Commerce T1/T2 / CPI architect | C-17; order read model | Open — define provider, consumer, request direction, data direction, endpoint and refresh/caching semantics |
| **Q-065** | **High** | **What uniquely identifies and versions one Submit-MIGO receipt position?** The validated screen is at DI/invoice/LR-GR/product grain and supports repeated partial postings, but the visual examples reuse the same visible references across rows. Is `DeliveryDocument` alone guaranteed to identify one SAP item, or must the command carry delivery item/PO/material or an opaque receipt-position key? What stale-state token or expected pending quantity prevents two users posting against the same old balance? | MM / ABAP / DSP / Commerce | API-01 key, locking, concurrency and idempotency | Open — D-054 proves that prior inward and current allocation are separate deltas. `RequestId` prevents replay of one attempt but does not prevent two different requests from consuming the same pending quantity. SAP must lock/re-read; the contract needs the authoritative business key and stale-state behavior |
| **Q-066** | **Critical** | **Which “pending” ledger receives rejected `DMG` and `STG` quantity?** Does it remain pending against the original DI/Pending-MRN receipt position, return to Sales Order pending quantity for creation of a new DI, or create a dedicated replacement/claim balance? Which system persists the rejection classification if no stock is stored? | MM / SD / Commerce / DSP | API-01 response, Pending-MRN derivation, order balance, replacement DI lifecycle | Open — `SRC-SID-20260811-07` establishes the business outcome: DMG/STG do not increase stock and require a later Order→DI→MIGO cycle. The target balance and document linkage determine the exact calculation and cannot be inferred from the screen |
| **Q-067** | **Critical** | Can each selected service return local `$metadata`, a representative safe read, and—where applicable—an authorised write that creates/changes the expected SAP document? | Basis / Security / ABAP / functional | API certification | **Partially answered 2026-08-25 onward (D-062/D-063):** representative stock and STO reads, STO Create DI, and one PO+delivery Submit MIGO are proven. Billing is read-only; STO create through the standard PO service is structurally rejected; PGI, Trade/Non-trade DI, delivery-only MIGO and the remaining custom command paths are not certified. Enhancement equivalence is now a separate mandatory gate |
| **Q-068** | **Critical** | Are SAP application-internal/Fiori services permitted as CPI integration contracts? Specifically, may `SD_CUSTOMER_INVOICES_CREATE` be used for billing creation, and may any MMIM service be used as more than a read-back aid? | Architecture / SAP product owner / Security | API-03 billing-create route and the conditional API-01 alternatives | Open — design-time evidence proves capability but not contract stability, release status or external-consumer support. A “no” requires the approved V4/BAPI/narrow-wrapper route |
| **Q-069** | **Critical** | What exact SAP object is “STO Invoice”: SD billing, intercompany billing, MM supplier invoice, or India GST stock-transfer document? | SD / MM / FI / Tax | API-10 service selection and joins | Open — `API_BILLING_DOCUMENT` is primary only for an SD billing object; the functional label alone cannot select the source |
| **Q-070** | **High** | Why is `ShippingType` mandatory on API-11 Create STO PO when `API_PURCHASEORDER_PROCESS` exposes no PO field for it? | MM / SD / Product | API-11 contract freeze | Open — decide whether SAP derives it, the field belongs on the subsequent outbound delivery, or it should be removed from API-11. Do not invent a custom PO extension without this decision |
| **Q-071** | **Critical** | Which existing SAP/EY/eDocument objects implement E-Way extension and invoice correction/cancel/regenerate, and what is their approved callable boundary? | Tax / ABAP lead / CPI / Basis | API-06 and API-07 solution | Open — the `EY_*` destinations prove infrastructure, not the function module/BAdI/service or request/response contract. Trace the live path before building a parallel endpoint |
| **Q-072** | **Critical** | Which exact reference contract must Submit MIGO support at the public boundary, and how is a delivery-only request mapped through the standard OData allowed-field layer into the BAPI delivery-search fields? | MM / ABAP / Architect | MIGO-02 contract and implementation | Working direction is delivery + item, not PO. Direct BAPI client code proves the pattern; the equivalent standard OData request and supported enhancement path remain unproven |
| **Q-073** | **Critical** | Which MIGO, goods-movement and yard validations are mandatory for the C&F API, and which must be refactored below the transaction/BAPI split so both paths enforce one implementation? | MM / ABAP lead / LE | MIGO-02 production readiness | Eight dialog-side customisation points are known; five sources/configuration areas remain incomplete and the active switches in `ZGPT_MM_PARAM` were not captured |
| **Q-074** | **High** | Who owns the three-day in-transit/MIGO-incomplete alert and through which system and notification channel is it delivered? | Architect / Product / CPI | Monitoring and operational completion | Raised in the 27 Aug Postman review; owner and implementation route remain open |
| **Q-075** | **High** | What are the authoritative rail/RSD receiving rules, including the meaning and applicability of `GDRK`, `RSD`, transport mode and storage-location derivation? | MM / LE | MIGO-02 validation and test coverage | Client code shows conditional rules; the API spike's hardcoded `RMYD` default is unsafe outside the proven road cases |
| **Q-076** | **Medium** | What business document or status did the reviewers call “W/WD,” and what end-user explanation and SAP field mapping applies? | MM / FI | Demo and error/response terminology | Unresolved transcription term from the 27 Aug review; do not guess |
| **Q-077** | **Critical** | How are replay, concurrent submissions and stale pending quantity handled for Create DI and Submit MIGO in QS4? | Architect / ABAP / Basis / CPI | Write safety | DS4 standard repeat-request setup is complete, QS4 is not configured, and no sequential/concurrent business POST proof exists. Request-ID replay protection must be combined with business-key locking and live re-read |
| **Q-078** | **Critical** | Why did active implementation `ZCNF_SUBMIT_MIGO` on `MB_BAPI_GOODSMVT_CREATE` not execute during the spike, and what exact class/interface registration and transport state is present in DS4 and QS4? | ABAP lead / Basis | Supported MIGO enhancement route | Current class is empty; earlier probe behavior was not observed. Resolve dispatch before writing production logic |
| **Q-079** | **High** | Have Create DI and PGI been audited for transaction-only customer exits, BAdIs, source plug-ins and configuration gates that the standard service path bypasses? | SD / ABAP lead | OF-01 and OF-05 business certification | STO Create DI persistence is proven and PGI metadata exists, but neither establishes equivalence to all client-specific transaction behavior |

## Question discipline

Ask questions in business-impact order. Do not ask for class names while the document model or source ownership is unknown.
